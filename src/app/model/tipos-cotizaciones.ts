export class tiposCotizaciones{
    public nombre: string;
    public nuevas: number;

    constructor(nombre: string, nuevas: number) {
        this.nombre = nombre;
        this.nuevas = nuevas;
    }
}